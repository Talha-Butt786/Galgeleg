<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.galgeleg">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ListActivity"
            android:screenOrientation="portrait"
            android:label="Click on a word to select"
            ></activity>
        <activity android:name=".LoadingActivity" android:screenOrientation="portrait" />
        <activity android:name=".LoseActivity" android:screenOrientation="portrait"/>
        <activity android:name=".ScoreListActivity" android:screenOrientation="portrait"/>
        <activity android:name=".SpilActivity" android:screenOrientation="portrait"/>
        <activity android:name=".SelectActivity" android:screenOrientation="portrait"/>
        <activity
            android:name=".WinActivity"
            android:parentActivityName=".MainActivity" android:screenOrientation="portrait"/>
        <activity android:name=".MainActivity"
            android:screenOrientation="portrait"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>